services:
  - type: web
    name: youtube-downloader
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
